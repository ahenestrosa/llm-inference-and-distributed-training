[project]
name = "llm-inference-and-distributed-training"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.6",
    "transformers>=4.48",
    "accelerate>=1.2",
    "bitsandbytes>=0.45",
    "autoawq",
    "peft",
    "datasets",
    "wandb",
    "matplotlib",
    "pandas",
    "optimum>=2.1.0",
    "flash-attn>=2.6; sys_platform == 'linux'",
]

[dependency-groups]
dev = [
    "pyright",
    "ruff",
]

[tool.uv]
environments = [
    "sys_platform == 'darwin' and platform_machine == 'arm64'",
    "sys_platform == 'linux'",
]

# flash-attn's setup.py imports torch at build time.
no-build-isolation-package = ["flash-attn"]

override-dependencies = [
    "triton>=3.0; sys_platform == 'linux'",
]

